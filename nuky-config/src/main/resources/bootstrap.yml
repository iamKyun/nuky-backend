server:
  port: 8000
spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri:
          username:
          password:

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
