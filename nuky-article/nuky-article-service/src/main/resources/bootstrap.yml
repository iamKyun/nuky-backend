eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

spring:
  application:
    name: article
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG
      profile: dev
  jpa:
    properties:
      hibernate:
        for
